export const RINKEBY = 'rinkeby';

export const CONTEXT = {
  COLONY: 'colony',
  NETWORK: 'network',
  TOKEN: 'token',
};

export const ACTION = {
  LIST: ['list', 'ls', 'l'],
};

/*
 * Process exit codes
 */
export const STATUS = {
  CANNOT_EXECUTE: 127,
};
